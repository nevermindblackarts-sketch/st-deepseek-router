# DeepSeek Router（DSH Standard 移植版）使用说明

界面为中文默认；酒馆语言设为 English 时自动切换为英文界面。

## 快速开始

1. 酒馆 → 扩展（Extensions）→ Install extension，填入仓库地址，刷新页面。
2. 接口要求：**Chat Completion 类**（Custom OpenAI-compatible / OpenAI 等）。Text Completion 源不经过本扩展的钩子，完全不会生效。
3. 默认即为「自动」模式且总开关已打开，开箱即用：每个聊天首次生成时，取首条用户消息分类出档位，锁定并注入对应 persona。
4. 面板顶部的状态栏会实时显示：**模型族 / 路由 / 当前 persona**。发一条消息后即可确认是否生效（见下方 FAQ「显示未生效」）。

## 面板各项说明

| 设置项 | 说明 |
| --- | --- |
| 启用插件（总开关） | 插件的总开关，关闭后所有注入与改写全部停止（标题旁的徽标会显示 启用中 / 已停用）。模式下拉里的旧「关闭」选项已并入此开关，旧设置自动迁移 |
| 路由模式 | 五档：自动 / 深度（spec）/ 快循环（react）/ 轻任务（weak）/ RL 接口还原（standard）。自动 = 按聊天首条用户消息分类并锁定；手动档 = 跳过分类直接钉死，对所有聊天生效 |
| Persona 注入位置 | 「最前的 system 消息」= persona 放在整个提示的最开头，锚定力最强（默认）；「系统提示块之后」= 放在角色卡等系统块后面、聊天记录前面，不抢主位 |
| 反空转锚点 | 距上下文窗口尾部 8 / 20 / 36 条用户消息处，追加一句 `[n]: ...` 锚点，抑制原地打转。**仅 DeepSeek Pro / Flash 族有锚点句**（模型 ID 需含 deepseek + pro / flash），其他族无此机制 |
| 深度自适应收敛引导 | 窗口快耗尽时，在**最后一条用户消息**末尾追加引导行（`[router]: ...`，操作约束放 user 面、不污染 system 面的 persona 锚）：轻任务/快循环档 → "立即收敛到最终答案，不再探索"；深度档 → "先用一句话写下最终决定，然后直接执行"。约在窗口深度 68%（快循环）和 88%（深度）时触发。`deepseek-chat` / `deepseek-reasoner` 也会触发 |
| 思维链格式约束 | **默认关闭**（实验性，来自内测反馈）。开启后对思考型模型（reasoner / thinking / r1 / v4 系列）在最后一条用户消息末尾追加约束块：限定思考开头为断言式短语（I'm / I've / I'll / The / Looking 等）、禁用 "Let me / Let's" 踟躇语气、禁止重复输出思考块。约束文本可在下方折叠区自定义（留空用内置） |
| 输出格式守护 | **开了本插件后模型忘记输出生图 tag / 状态栏等世界书格式义务时开启**。原理：persona 是强身份锚，会压过低显著度的世界书格式指令；本开关在最后一条用户消息末尾追加一行高显著度提醒，要求模型收尾前回查并执行上下文中的全部格式指令。守护文本可自定义（留空用内置） |
| 对非 DeepSeek 模型也生效 | 默认只路由 DeepSeek 系（含 kimi / qwen / kiro / minimax 等已知族）。勾选后所有模型都注入 persona，适合自己试验其他模型 |
| Persona 覆写 | 三个档位各可自定义一句话。留空 = 使用按模型族配好的内置句 |
| 重新分类本聊天 | 清除当前聊天已锁定的分类结果，下次生成时按首条用户消息重新分类。改完开场白或分类分错了就点它 |
| 恢复默认设置 | 全部设置回到默认并刷新页面 |

## 模式怎么选（按用途）

- **日常 RP 聊天**：自动 或 手动「快循环」。RP 开场白大多会分到快循环/轻任务档。
- **长篇创作、复杂世界观推演、设定整理**：手动「深度（spec）」。
- **起名、总结、问答这类轻活**：自动即可；不满意可手动「轻任务（weak）」。
- **纯助手/工具型用法（无角色卡）**：可试「RL 接口还原」——所有 system 消息被替换为 RL 训练句 `You are a helpful software engineer assistant.`。
  ⚠️ **角色卡不会到达模型，RP 聊天绝对不要开**。

## Persona 覆写怎么写

内置 persona 是在编码负载上测得的，酒馆 RP 场景建议按"档位力度"自己改写，机制不变。要点：

1. **一句话**，陈述句，身份式表述——persona 的作用力来自"身份认同"，不是规则堆砌；
2. 语气力度对准档位：spec 档要"缜密、重细节"，react 档要"果断、即刻推进"，weak 档要"简洁"；
3. 建议用英文写（机制源自英文测量，效果更稳定），例如 react 档 RP 向：

   ```
   You are a vivid novelist who commits to the scene and moves it forward.
   ```

状态栏的 Persona 一行会实时预览当前生效的句子。

## FAQ

**标题旁显示「已停用」？**
总开关没打开。展开本插件面板，勾选第一项「启用插件（总开关）」即可。

**状态栏显示「未生效」？**
状态栏会附带原因：`未生效·接口非 Chat Completion` 说明接口类型不对；`未生效·<模型ID>` 说明当前模型没被识别为 DeepSeek 系——模型 ID 需含 `deepseek` 字样（自定义接口给模型起了不含 deepseek 的别名时，需勾选「对非 DeepSeek 模型也生效」强制开启）。另外空聊天（还没有用户消息）不路由。模型名从当前接口源的模型设置读取（Custom 源的自定义模型名、DeepSeek 专用源、OpenRouter / SiliconFlow 等的已选模型均支持）。

**为什么没有锚点？**
锚点句只配给了 DeepSeek Pro / Flash 族（上游只对这两族做过测量）。`deepseek-chat` / `deepseek-reasoner` 映射到其他族，persona 用核心 RL 句、收敛引导正常，但没有锚点。

**分类分错了怎么办？**
手动钉死模式，或点「重新分类本聊天」。

**开了插件后模型不输出生图 tag / 状态栏了？**
这是 persona 身份锚压过了世界书格式指令（格式义务显著度低，是身份切换的第一批牺牲品）。两条路任选或叠加：① 打开「输出格式守护」开关（user 面追加一行收尾回查提醒，见面板说明）；② 在 Persona 覆写里把对应档位改成保留格式职责的叙事句，例如 react 档：`You are a vivid novelist who commits to the scene and follows every output-format instruction in the context exactly.`。若同时开了思维链格式约束，建议先关它单独排查。

**思维链格式约束什么时候开？**
只用思考型模型（deepseek-reasoner、r1、v4 系列等）且遇到思考链跑偏/原地打转/思考块重复输出时再开。它是概率性偏置不是硬约束，开头白名单的普适性还在内测验证中；非思考模型（deepseek-chat）开了也不注入，不浪费 token。

**会不会影响标题生成、总结、翻译这类后台功能？**
不会。插件只作用于用户可见的聊天回合；扩展的后台静默请求（标题/总结/翻译/情绪识别等）和裸补全调用（正则测试器、快速问 AI 等）会被识别并原样放行。

**会不会改动我的聊天记录？**
不会。所有注入只发生在发送给模型的提示上，已存储的消息原样保留；卸载后无残留影响（聊天 metadata 里会留一个 `dsh_router_state` 字段，无害）。

**Persona 为什么是英文？**
它是发给模型的提示词，不是界面文字。英文表述效果更稳定；覆写时也建议英文。

**和「合并系统消息」（squash system messages）冲突吗？**
不冲突。合并发生在本扩展注入之前，注入的 system 消息不会被合并掉。

**怎么更新？**
manifest 已开启 `auto_update`，扩展面板的更新检查会自动拉取新版本；手动更新可在扩展面板点更新或重新 Install 一次。

## 卸载

扩展面板中删除本扩展即可。
